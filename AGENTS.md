# Cafe24 Project Instructions

이 파일은 이 저장소에서 작업하는 모든 LLM/코딩 에이전트가 작업 전에 따라야 하는 프로젝트 규칙이다.
카페24 관련 작업에서는 일반적인 정적 웹사이트 관행보다 이 문서의 카페24 제약을 우선한다.

## 1. 프로젝트와 배포 대상

- 쇼핑몰: `https://tkddls8848.cafe24.com/`
- 작업 대상 스킨: `skin5`
- 원격 디자인 경로: `/sde_design/skin5`
- 원본 작업본: `cafe24-skin-work/skin5`
- FTP 배포본: `manual-file-upload/skin5`
- 배포 실행기: `UPLOAD-SKIN5.cmd`
- WinSCP 저장 세션: `tkddls8848@ecimg-ftp-c01.cafe24img.com`

라이브 사이트에 반영됐다고 말하기 전에 실제 원격 업로드 또는 라이브 URL 검증이 완료되었는지 반드시 구분한다.
로컬 파일 수정만으로는 라이브 사이트에 적용되지 않는다.

## 2. 카페24 백업과 배포 정책

- 카페24에서 내려받은 스킨 백업 `.tar.gz`는 복원용 원본이다.
- 백업 압축파일의 내부 파일, 형식, 메타데이터를 수정하거나 재압축하여 배포 패키지로 사용하지 않는다.
- 수정한 `.tar.gz`를 관리자 복원 기능에 업로드하는 방법을 제안하지 않는다.
- 스킨 코드 배포는 카페24 `디자인FTP`를 사용한다.
- `디자인FTP`는 **SFTP 전용**이다. 일반 FTP나 웹 FTP와 혼동하지 않는다.
- SFTP 호스트와 포트는 카페24 관리자 `디자인(PC/모바일) > 디자인FTP`의 권한 신청 화면에 표시된 값을 사용한다.
- 디자인FTP 권한에는 만료시간이 있으므로 접속 또는 쓰기 실패 시 만료 여부를 먼저 확인한다.
- 비밀번호를 코드, 문서, 로그, 명령행 인수 또는 채팅에 저장하거나 출력하지 않는다.

공식 참고:

- 카페24 FTP 이용: https://support.cafe24.com/hc/ko/articles/17413444413209
- 카페24 디자인FTP/SFTP: https://support.cafe24.com/hc/ko/articles/7751150158873
- 카페24 스킨 수동 이전: https://support.cafe24.com/hc/ko/articles/8466116839833

## 3. 카페24 파일 제한

- 확장자 없는 파일은 업로드하지 않는다. 카페24가 퍼미션 오류처럼 거부할 수 있다.
- HTML, CSS, JS, TXT, XML, 이미지, 웹폰트 등 카페24가 허용하는 확장자만 사용한다.
- 파일명과 폴더명은 영문, 숫자, 하이픈, 밑줄 위주로 작성한다.
- `preference` 폴더와 INI 설정 파일은 FTP 전송 제한 대상이므로 수정 또는 업로드 대상으로 잡지 않는다.
- `/sde_design` 루트가 아닌 `/sde_design/skin5` 내부의 기존 구조에 파일을 배치한다.
- 로컬 `skin5` 폴더 자체를 이미 열린 원격 `/sde_design/skin5` 안으로 넣어 `/skin5/skin5`가 생기게 하지 않는다.

## 4. 수정 파일 동기화 규칙

FTP 배포본은 전체 스킨이 아니라 아래 변경 파일 7개만 포함한다.

1. `index.html`
2. `layout/basic/layout.html`
3. `layout/basic/css/mn-home.css`
4. `layout/basic/css/mn-theme.css`
5. `modules/header/html/header.html`
6. `modules/top_banner/html/text_fixed.html`
7. `SkinImg/content/mn-hero-office.jpg`

CSS를 수정할 때는 원본 작업본과 FTP 배포본을 함께 갱신한다.

- 원본 `modules/mirnuri_office/css/theme.css` ↔ 배포본 `layout/basic/css/mn-theme.css`
- 원본 `modules/mirnuri_office/css/home.css` ↔ 배포본 `layout/basic/css/mn-home.css`
- 원본 히어로 이미지 자리표시자 `__MN_HERO_DATA__`는 원본 패키징 용도다.
- FTP 배포본의 히어로 URL은 `/SkinImg/content/mn-hero-office.jpg`여야 한다.
- FTP 배포본에 `__MN_HERO_DATA__`가 남아 있으면 배포하지 않는다.

명시적인 사용자 요청 없이 전체 스킨 동기화, 원격 파일 삭제, 원격 폴더 삭제를 하지 않는다.

## 5. 카페24 CSS 호환성

카페24 기본 스킨 CSS는 높은 선택자 우선순위와 오래된 레이아웃 규칙을 포함한다.
새 CSS가 파일 뒤쪽에 있다는 이유만으로 적용된다고 가정하지 않는다.

특히 다음 기본 규칙과 충돌 여부를 확인한다.

- `#container`의 `max-width: 1600px`
- `#header.layout3` 관련 `display`, `order`, `height`, `padding`, `position`
- `.top_nav_box`, `.logo_area`, `.top_logo`, `.top_mypage`
- 모바일/데스크톱 미디어쿼리
- SVG 내부 `path`의 인라인 `stroke` 또는 `fill`

규칙:

- 우선 실제 라이브 페이지의 `getComputedStyle`과 `getBoundingClientRect()`를 검사한다.
- `!important`는 확인된 카페24 충돌을 이기는 데 필요한 좁은 선택자에만 사용한다.
- 전역 태그나 모든 링크에 광범위한 `!important`를 사용하지 않는다.
- 메인 페이지는 `#wrap`, `#container`, `#contents`의 폭 제한 때문에 오른쪽 공백이 생기지 않도록 한다.
- 레이아웃 수정 후 가로 스크롤, 겹침, 헤더 밖 이탈을 확인한다.

## 6. 헤더 승인 기준

데스크톱 폭에서는 헤더를 반드시 한 줄로 유지한다.

- 카테고리 메뉴: 왼쪽 정렬
- 브랜드 로고: 중앙 정렬
- 로그인/회원가입/검색/마이페이지/장바구니: 오른쪽 정렬
- 모든 헤더 텍스트와 아이콘은 배경과 충분한 대비를 가져야 한다.
- 카테고리와 계정 영역이 히어로 위로 내려가거나 헤더 밖으로 나가면 실패다.
- 메뉴 항목 간 간격과 자간은 조밀하지 않게 유지한다.
- 1100px 이하에서만 데스크톱 카테고리를 숨기고 메뉴 버튼으로 전환한다.

최소 검증 폭:

- 1920px 데스크톱
- 1166px 소형 데스크톱/노트북
- 390px 모바일

## 7. 시각 디자인 원칙

- 방향: 모던, 심플, 미니멀한 사무용품 쇼핑몰.
- 네이비, 웜그레이, 화이트를 기본으로 사용한다.
- 참고 사이트의 구조와 분위기는 참고하되 그대로 복제하지 않는다.
- 장식보다 정보 위계, 가독성, 간격, 정렬을 우선한다.
- 히어로 이미지는 화면 폭을 채우되 본문 텍스트를 가리는 과도한 카드나 오버레이를 피한다.
- 둥근 모서리와 그림자는 절제해서 사용한다.
- 사용자가 제공한 스크린샷의 문제를 수정할 때는 수정 전 원인을 계산값으로 확인한다.

## 8. 필수 검증 절차

디자인 변경 후 다음 순서를 생략하지 않는다.

1. 수정한 CSS의 중괄호 균형과 필수 파일 존재 여부를 검사한다.
2. `manual-file-upload/skin5`에 히어로 자리표시자가 남지 않았는지 검사한다.
3. `UPLOAD-SKIN5.ps1 -CheckOnly`로 7개 배포 파일을 검사한다.
4. 가능하면 `tools/preview_live_css.mjs`로 라이브 HTML에 로컬 CSS를 임시 주입해 서버 업로드 전에 캡처한다.
5. 1920px와 1166px에서 헤더 요소의 좌표, 색상, 겹침을 확인한다.
6. 실제 업로드 후 라이브 URL을 새 쿼리 문자열 또는 강력 새로고침으로 확인한다.
7. 사용자에게 로컬 수정 완료와 라이브 적용 완료를 구분해 보고한다.

## 9. 배포 절차

- 사용자가 카페24에서 디자인FTP 권한을 신청하고 비밀번호를 직접 입력한다.
- 자동 업로드는 `UPLOAD-SKIN5.cmd`를 사용한다.
- 자동화는 지정된 7개 파일만 `/sde_design/skin5`의 정확한 상대경로로 전송해야 한다.
- WinSCP 종료 코드가 0인지 확인하고 `upload-logs/winscp-latest.log`를 남긴다.
- 업로드 실패 시 반복 실행 전에 로그의 첫 실제 오류를 확인한다.
- 적용 후 `https://tkddls8848.cafe24.com/`에서 캐시를 우회해 확인한다.
- `skin5`가 대표 디자인이 아니면 관리자 디자인 보관함에서 대표 디자인 여부를 확인한다.

## 10. 카페24 모듈 주의사항

- 메인 상품 영역은 `product_listmain_9` 모듈을 사용한다.
- 상품이 비어 있으면 HTML 오류로 단정하지 말고 카페24 관리자에서 메인 상품진열 9번에 상품이 연결됐는지 확인한다.
- 카페24 모듈 속성, 변수, `<!--@layout-->`, `<!--@css-->`, `<!--@import-->` 지시문을 일반 HTML 주석으로 보고 제거하지 않는다.
- `{$...}` 형태의 카페24 템플릿 변수를 임의의 고정값으로 바꾸지 않는다.

## 11. 보고 방식

- 실패 원인을 추측으로 단정하지 않는다. 로그, 실제 파일, 계산된 CSS 값을 근거로 설명한다.
- 사용자에게 비밀번호나 민감한 접속정보를 채팅으로 보내 달라고 요청하지 않는다.
- 적용되지 않은 상태에서 적용됐다고 말하지 않는다.
- 검증하지 못한 부분은 명확히 검증 전이라고 표시한다.
